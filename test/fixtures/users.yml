patient:
  first_name: Luke,
  last_name: Skywalker,
  created_at: <%= Time.zone.now %>

# Setting is_doctor stores the value as boolean in the database
# But calling User.default_doctor returns nil (scope is nil)
# Calling User.where(is_admin: true) returns nil as well
# Same goes for the Admin
# I will update the roles fields(is_admin, is_doctor) in my test setup

doctor:
  first_name: Leia,
  last_name: Organa,
  # is_doctor: 1,
  # is_patient: 0
  created_at: <%= Time.zone.now %>

admin:
  first_name: Obi-wan,
  last_name: Kenobi,
  # is_admin: 1,
  # is_patient: 0,
  created_at: <%= Time.zone.now %>